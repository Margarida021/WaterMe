{
  "background_color": "white",
  "description": "Discover the ultimate plant care solution at Water Me! Our expert tips, innovative products, and personalized guidance ensure your plants thrive. Say goodbye to wilted leaves and hello to a green paradise. Start nurturing your botanical friends today!",
  "display": "fullscreen",
  "name": "WaterMe",
  "icons": [
    <% files = Dir.entries(Rails.root.join("app/assets/images/icons/")).select {|f| !File.directory? f} %>
    <% files.each_with_index do |file, index| %>
      <% match = file.match(/.+-(?<size>\d{2,3}x\d{2,3}).png/) %>
      {
        "src": "<%= image_path "icons/#{file}" %>",
        "sizes": "<%= match && match[:size] %>",
        "type": "image/png"
      }<%= "," unless (files.size - 1) == index %>
    <% end %>
  ]
}
